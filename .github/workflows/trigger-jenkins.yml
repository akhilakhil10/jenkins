name: Trigger Jenkins Job

on:
  push:
    branches:
      - master

jobs:
  trigger_jenkins_job:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Jenkins Job
        run: |
          http -a admin:admin POST "http://34.88.175.96:8080/job/Trigger%20Airflow%20DAG/build?token=jenkins_job" X-Requested-By:Jenkins
